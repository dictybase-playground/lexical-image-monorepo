import { makeStyles } from "@material-ui/core"

const useImageMenuStyles = makeStyles({
  root: {
    position: "absolute",
    bottom: "5%",
    zIndex: 1,
  },
})

export default useImageMenuStyles
