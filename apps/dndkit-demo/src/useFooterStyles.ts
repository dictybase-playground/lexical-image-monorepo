import { makeStyles } from "@material-ui/core"

const useFooterStyles = makeStyles({
  root: {
    color: "hsl(0, 0%, 100%)",
    backgroundColor: "hsl(210, 100%, 25.1%)",
    minHeight: "150px",
    paddingTop: "10px",
  },
})

export default useFooterStyles
